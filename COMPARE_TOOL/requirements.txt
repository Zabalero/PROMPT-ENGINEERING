# File Comparison Tool Requirements

# Optional dependency for better encoding detection
chardet>=5.0.0

# All other dependencies are built into Python standard library:
# - tkinter (GUI framework)
# - difflib (text similarity)
# - os (file operations)
# - typing (type hints)